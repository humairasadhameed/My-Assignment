function make_shirt(size: string, message: string): void {
    console.log(`Made a ${size} shirt with the message: "${message}".`);
}

// Example call to the function
make_shirt('medium', 'Hello, TypeScript!');
